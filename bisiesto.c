/*
    bisiesto.c
    Valor: 1 punto
*/

/*
    Elabore un programa donde el usuario ingresa el anio y este le responde si es bisiesto o no. Las reglas para saber si un anio es bisiesto son las siguientes:
    - Los anios que son divisibles entre 4 pero no entre 100 son bisiestos.
    - Los anios que son divisibles entre 100 pero no entre 400 no son bisiestos.
    - Los anios que son divisibles entre 400 son bisiestos.

    La funcion bisiesto() tiene como parametro a year, que representa el anio a evaluar

    Ejemplos:
    Estos anios son bisiestos porque son divisibles entre 4
    1996, 2004

    Estos anios no son bisiestos, son divisibles entre 4 pero tambien entre 100
    2100, 1900

    Estos anios si son bisiestos porque son divisibles entre 400
    200, 1600

    Estos anios no son bisiestos porque no cumplen con ninguno de los criterios
    2001, 1999
*/

#include <stdio.h>

int bisiesto (int year) {
    return 0;
}

int main () {
    return 0;
}
