/*
    tiro.c
    Valor: 3 puntos
*/

/*
    La seleccion de tiro nos pidio un programa para ayudar a los atletas con su punteria. El atleta dispara hacia un objetivo circular movil, nuestro programa debe imprimir si el tiro quedo dentro del circulo o no. 
    
    La funcion tiro() tiene estos parametros
    Coordenadas x, y del centro del circulo
    Radio del circulo
    Coordenadas x, y del tiro

    Algunos casos de prueba:
    
    Circulo x=0, y=0, radio=5
    Punto x=3, y=4
    El tiro quedo DENTRO del circulo

    Circulo x=0, y=0, radio=5
    Punto x=6, y=0
    El tiro quedo FUERA del circulo

    Circulo x=2 y=3, radio=4
    Punto x=5, y=6
    El tiro quedo FUERA del circulo

    Circulo x=2 y=3, radio=4
    Punto x=5, y=5
    El tiro quedo DENTRO del circulo
*/

#include <stdio.h>
#include <math.h>

int tiro (int centro_x, int centro_y, int radio, int tiro_x, int tiro_y) {
    return 0;
}

int main () {
    double ejemplo = sqrt(25.0);    // raiz cuadrada, da como resultado un double
    return 0;
}
