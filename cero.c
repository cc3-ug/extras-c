/*
    cero.c
    Valor: 2 puntos
*/

/*
    Escriba un programa que indique cuantos ceros hay en un numero

    La funcion cero() tiene como parametro num, que representa el numero a evaluar

    Ejemplos:
    5005 debe desplegar que tiene 2 ceros
    123045 debe desplegar que tiene 1 cero
    999000 debe desplegar que tiene 3 ceros
    8989 debe desplegar que no tiene ceros
*/

#include <stdio.h>

int cero (int num) {
    return 0;
}

int main () {
    return 0;
}
